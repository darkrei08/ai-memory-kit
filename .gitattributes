# Keep shell scripts LF so they stay executable on Linux/macOS regardless of the
# committer's platform (Windows autocrlf must not touch these).
*.sh        text eol=lf
bin/aimem   text eol=lf
hooks/*     text eol=lf
*.ps1       text eol=crlf

# Everything else: normalized text.
*           text=auto
